%article
  %h1= @post.title
  != @post.publication_date_in_html
  = render :partial => 'image', :locals => { :post => @post,
                                             :image_link => @post.image.url }
  :markdown
    #{@post.body}

= link_to "Post index", posts_path
